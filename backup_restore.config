# backup_restore.config
# Konfiguracja dla skryptów backup-cloud.sh i restore-cloud.sh (Wersja 5.5)

# --- GŁÓWNA KONFIGURACJA ---
# Login i nazwa repozytorium na GitHubie
GH_REPO=""

# Ścieżka do pliku logu. Zostaw PUSTE (""), aby wyłączyć logowanie do pliku.
LOG_FILE="$HOME/Desktop/backup_restore.log"

# E-mail powiązany z Twoim kluczem publicznym GPG.
# Zostaw PUSTE (""), aby wyłączyć szyfrowanie w backup-cloud.sh.
GPG_RECIPIENT_EMAIL=""

# --- KONFIGURACJA ZAWARTOSCI BACKUPU ---
# Lista plików i katalogów do dołączenia do kopii zapasowej.
# Używaj $HOME, aby ścieżki były przenośne.
BACKUP_PATHS=(
    # --- Podstawowa konfiguracja Shell ---
    "$HOME/.zshrc"
    "$HOME/.secrets"

    # --- Konfiguracja Powerlevel10k ---
    "$HOME/.p10k.zsh"

    # --- Konfiguracja narzędzi ---
    "$HOME/.config/amass"
    "$HOME/.config/subfinder"

    # --- Kompletna konfiguracja Warp Terminal ---
    # Obejmuje motywy, skróty klawiszowe, historię, itp.
    "$HOME/.config/warp-terminal"
    "$HOME/.local/state/warp-terminal"
)

# Lista narzędzi Go do backupu. Skrypt spróbuje odczytać ich dokładną wersję.
# Jeśli się nie uda, zapisze tę ścieżkę jako fallback do instalacji.
GO_TOOLS_TO_BACKUP=(
    "github.com/owasp-amass/amass/v4/cmd/amass@latest"
    "github.com/projectdiscovery/subfinder/v2/cmd/subfinder@latest"
)
